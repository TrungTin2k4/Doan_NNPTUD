export default function Home() {
    return (<main style={{ fontFamily: "sans-serif", padding: "2rem", lineHeight: 1.6 }}>
      <h1>EduLearn Next.js Backend</h1>
      <p>Backend API is running.</p>
      <p>Use endpoints under /api/*.</p>
    </main>);
}
